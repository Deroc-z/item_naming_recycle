from setuptools import setup, find_packages

setup(
    name="item_naming_recycle",
    version="1.0.0",
    description="Item naming with automatic code recycling for ERPNext",
    author="Your Name",
    author_email="your@email.com",
    packages=find_packages(),
    zip_safe=False,
    include_package_data=True,
    install_requires=[],
)
